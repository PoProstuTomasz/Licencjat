<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poprostutomasz07.licencjat">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_hospital_grey"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AutorActivity" />
        <activity android:name=".AnkietaActivity" />
        <activity android:name=".StatystykiActivity" />
        <activity android:name=".AplikacjaActivity"></activity>
    </application>

</manifest>